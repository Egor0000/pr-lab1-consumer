spring:
  application:
    name: ${APP_NAME:pr-lab1-consumer-dev}

server:
  address: ${ADDRESS:0.0.0.0}
  port: ${PORT:8080}

producer:
  address: ${PRODUCER_ADDRESS:http://localhost}
  port: ${PRODUCER_PORT:8070}

kitchen:
  time:
    unit: ${KITCHEN_TIME_UNIT:1}
    duration: ${KITCHEN_TIME_DURATION:100}
  cooks: ${COOKS_COUNT:2}